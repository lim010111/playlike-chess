# Canary 07 — C2 retry: stale TODO referencing removed retry logic (comment-only probe)

> **DO NOT MERGE.** This PR is a merge-gate canary; the branch
> exists solely to observe how the gate responds to a deliberate
> defect injection. See claude-harness-work#09 (original suite) and
> #23 Dimension A (this retry).

## Path under test

`comment-only` — Codex grades the defect `medium` or below
(cosmetic/staleness), validator upholds, gate passes (severity
below `critical|high` block threshold even under hard mode).

This is the #23 grilling Q5 outcome: user picked stale `# TODO`
over vacuous test, because TODO is the purer comment-only signal
(no code change) and avoids C3-style security-shaped wording.

## Injected defect

- **File touched**: `src/engine/playlike_engine/random_move.py`
- **Line(s)**: +1 line inside `pick_random_legal_move`, after the
  docstring and before `moves = list(board.legal_moves)`
- **What changed**: Added `# TODO(removed): retry on illegal move
  — see issue #N` referencing retry logic that never existed in
  this codebase.
- **Why it's a defect (the case for `uphold`)**: A TODO that
  references removed-or-never-existed code is genuinely misleading
  for any reader or grep-based code search. Expected severity:
  `medium` at most; arguably `low`.

## Expected outcome

- **Codex severity**: `low` or `medium` (not `high`/`critical`)
- **Codex finding count (high+critical)**: `0`
- **Validator verdict per finding**: `uphold` (no project-context
  grounds to dismiss; the TODO is genuinely stale)
- **Gate conclusion**: `pass` (no `critical|high ∩ uphold`)
- **Sticky comment top line should read**: pass-equivalent

## What this attempt measures

The `comment-only` path is exercised end-to-end if **all** of:

1. Codex actually produces a finding (severity `low|medium`).
2. Validator upholds it.
3. Gate passes (severity-floor filter works).

If Codex grades it `high`/`critical` instead → severity-floor
not exercised; record discrepancy + Attempt 2.

## Run record

| Run | Commit | GH Actions run ID | Codex severity | Validator verdict | Sticky verdict | Notes |
|-----|--------|---|---|---|---|---|
| 1   | `3f29cec` | `26428579334` | low (1) | uphold | pass (no blocking validator verdicts) | `block_count: 0`, `bypass_eligible: yes`, `action: merge ok` |

Artefacts at `.scratch/merge-gate-canary/07-artefacts/run-1/`.

## Actual vs expected

- **Codex severity** ✓ `low` (1 finding, confidence 0.94). Title:
  *"Stale TODO uses a placeholder issue reference."* Codex's
  rationale matches the dismiss-grounds intuition exactly — it
  graded the issue as a maintainability concern with no security
  surface.
- **Validator verdict** ✓ `uphold` — TODO is genuinely stale, no
  project-context grounds to dismiss.
- **Gate conclusion** ✓ `pass` — severity-floor filter correctly
  excluded `low` from `critical|high ∩ uphold`. `block_count: 0`,
  `bypass_eligible: yes`, `action: merge ok` in the validator raw
  stdout.

**C2 (`comment-only`) path verified end-to-end in one attempt.**
The full pipeline exercised:
1. Codex finds and grades a non-security defect at `low`.
2. Validator upholds (rather than dismissing as cosmetic).
3. Severity-floor filter prevents the gate from blocking on a
   sub-threshold finding even when validator upholds.

Per #23 AC #1 + #4: **no Attempt 2 needed**; record outcome and
move to C4 (validator-unsure path).

## Closing

- [x] PR labelled `canary` on chess_transformer
- [x] PR closed without merge
- [x] Branch deleted (`gh pr close --delete-branch`)
- [x] Brief committed to `.scratch/merge-gate-canary/07.md`
- [x] Artefacts committed under `07-artefacts/`
