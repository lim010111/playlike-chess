# Canary 05 — C3 retry attempt 1: fake `verify_auth_token` helper, no gate (validator-dismiss probe)

> **DO NOT MERGE.** This PR is a merge-gate canary; the branch
> exists solely to observe how the gate responds to a deliberate
> defect injection. See claude-harness-work#09 (original suite) and
> #23 Dimension A (this retry).

## Path under test

`validator-dismiss` — Codex grades high/critical on the bare auth
helper, validator dismisses citing explicit project-context grounds,
gate passes (no `critical|high ∩ uphold`).

This is the staged-Medium first attempt per #23 grilling tree
(Q3 outcome: Medium-first, no gate; INTENTIONAL inline comment
reserved for Attempt 2 if Medium misses).

## Injected defect

- **File touched**: `src/engine/playlike_engine/api.py`
- **Line(s)**: imports (+1 token `Header`); new module-level helper
  (+5 lines after `app = FastAPI(...)`); `/move` signature
  (+`authorization: str = Header(default="")`); body first line
  (`verify_auth_token(authorization)`, return value ignored)
- **What changed**:
  1. `Header` added to the FastAPI import line.
  2. Module-level `def verify_auth_token(token: str) -> bool: return True`
     with a one-line docstring.
  3. `/move` now accepts an optional `Authorization` header.
  4. First line of `move()` calls `verify_auth_token(authorization)`
     and discards the result.
- **Why it's a defect (the case for `uphold`)**: A function named
  `verify_auth_token` whose return value is **ignored** by the
  caller is the textbook broken-auth pattern — looks like a
  forgotten `if not verify_auth_token(...): raise 401`. Codex is
  expected to grade this `high` or `critical`.

## Dismiss grounds (the case for `dismiss`)

The validator should be able to find all three citations and
conclude that the engine has no client identity to authenticate;
`verify_auth_token` is v2-multiplayer scaffolding, decorative in v1.

- `src/engine/CONTEXT.md:36` — verbatim:
  > The server is **stateless per request**. Game state lives in
  > the browser (`chess.js`).
- `src/web/CONTEXT.md:36` — verbatim:
  > Session state lives **only in the browser**. Page refresh
  > discards it. No server-side persistence — see PRD Out of Scope
  > on multiplayer/accounts.
- `.scratch/playlike-chess/PRD.md:160` — PRD Out of Scope
  §multiplayer / matchmaking / accounts (paraphrase, not pseudo-quote).

Argument: ignored return value is not a real security defect when
the surface has no notion of a user to authenticate; the helper is
in-progress scaffolding for v2 multiplayer.

## Expected outcome

- **Codex severity**: `high` or `critical`
- **Codex finding count (high+critical)**: `≥ 1`
- **Validator verdict per finding**: `dismiss`
- **Gate conclusion**: `pass` (no `critical|high ∩ uphold`)
- **Sticky comment top line should read**: a pass-equivalent (the
  exact phrasing depends on the workflow's templating; the
  signal is "validator dismissed it, gate did not block")

## Risk

Validator's retrieval may or may not actually load
`src/web/CONTEXT.md` and `.scratch/playlike-chess/PRD.md`. If the
only citation it produces is `src/engine/CONTEXT.md`, dismiss may
be weaker — the *engine*-side stateless-per-request line argues
"no identity to authenticate," but the strongest dismiss requires
also citing the web-side "session is browser-owned" and the PRD's
explicit "no accounts in v1." The run result is the measurement.

## Run record

| Run | Commit | GH Actions run ID | Codex severity | Validator verdict | Sticky verdict | Notes |
|-----|--------|---|---|---|---|---|
| 1   | `4ba1beb` | `26427871804` | critical (1) | uphold | needs-attention (soft — would block under hard) | Validator cited only `api.py:35` + `:20` (own-file), no project-context citations |

Artefacts at `.scratch/merge-gate-canary/05-artefacts/run-1/`.

## Actual vs expected

- **Codex severity** ✓ critical — Codex graded the bare auth helper
  exactly as predicted (`confidence: 0.98`, finding titled
  *"Authorization is collected but never enforced"*).
- **Validator verdict** ✗ `uphold` (expected `dismiss`). Path miss.

**Hypothesis (1 — primary, evaluable from this run's output):**
The validator never produced a citation to
`src/engine/CONTEXT.md`, `src/web/CONTEXT.md`, or the PRD's
*Out of Scope §accounts* — it cited only two lines of `api.py`
itself (`:35` and `:20`). Two distinguishable sub-hypotheses:
1a. The validator agent's read window did not load those files,
    so the project-context dismiss grounds were not available
    at decision time.
1b. The validator did load them but γ Strict's "ignored return
    value" red flag dominated — i.e. even with the citation
    available, the calibration treats a function literally named
    `verify_auth_token` whose result is discarded as
    *security-load-bearing by name* over and above any
    stateless-per-request context.

These two are not yet distinguishable from this run alone.
Attempt 2 — Heavy + an inline `# INTENTIONAL: …` comment that
references `src/engine/CONTEXT.md` and the PRD directly *in the
diff itself* (γ Strict's documented intentionality trigger,
`.claude/agents/codex-review-validator.md:60-61`) — should help
disambiguate: if Attempt 2 still upholds, hypothesis 1b is
favoured (calibration-dominated); if it dismisses, 1a was the
binding constraint (retrieval was the issue, not calibration).

**Hypothesis (2 — secondary, less likely):** The "ignored return
value" inline observation is itself a γ Strict signal that
overrides any contextual dismissal regardless of citation
availability. Distinguishable from 1b only by comparing across
multiple sufficiently varied dismiss-candidate runs.

**Decision per #23 AC #4:** record discrepancy + proceed to
Attempt 2 (Heavy + INTENTIONAL inline comment, branch
`canary/06-c3-retry-heavy-intentional`, brief `06.md`).

## Closing

- [x] PR labelled `canary` on chess_transformer
- [x] PR closed without merge
- [x] Branch deleted (`gh pr close --delete-branch`)
- [x] Brief committed to `.scratch/merge-gate-canary/05.md`
- [x] Artefacts committed under `05-artefacts/`
