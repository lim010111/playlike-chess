# Canary 11 — Dim B observability: silent illegal-UCI rollback in requestEngineMove

> **DO NOT MERGE.** This PR is a merge-gate canary; the branch
> exists solely to observe how the gate responds to a deliberate
> defect injection. See claude-harness-work#23 Dimension B.

## Path under test

`comment-only` — Codex grades the defect `medium` (silent
failure / observability gap), validator upholds (`setError` is
the file's loud-on-failure pattern; `apiClient.ts` also surfaces
errors loudly), gate passes because severity-floor filters
`medium` out of `critical|high ∩ uphold`.

Category: **observability** (Dim B #3). Phase 2 surface exposes
this category through `useChessSession.ts:96–106` — the
illegal-UCI catch block calls `setError(new ApiError(...))`
explicitly so operators and users get a signal when the engine
returns a malformed move.

## Injected defect

- **File touched**: `src/web/app/hooks/useChessSession.ts`
- **Line(s)**: removed line 103 (was
  `setError(new ApiError(0, \`engine returned illegal move "${uci}"\`))`).
- **What changed**: a single-line deletion. The illegal-UCI
  catch branch still rolls back chess.js, syncs `fen`, slices
  `history`, and clears `awaiting` — but the *signal* that
  something went wrong is gone. The error surface is silenced
  while the recovery sequence runs cleanly.
- **Why it's a defect (the case for `uphold`)**: The original
  code is loud-on-purpose. After the deletion:
  - chess.js is rolled back (correct).
  - React `fen` reflects the rollback (correct).
  - `history` loses the user's move (correct).
  - `awaiting` clears (correct).
  - **But `error` stays at whatever it was before** — typically
    `null`. The UI shows no error banner, the operator sees no
    log signal, and from the user's perspective the move simply
    "didn't happen". A backend bug returning malformed UCI is
    invisible.
  Surrounding contract: `setError(new ApiError(...))` is the
  file's documented pattern for surfacing rejection (used in the
  422 branch too); `apiClient.ts` also surfaces errors loudly
  by raising `ApiError` rather than swallowing them. The
  deletion breaks this two-citation pattern.

## Expected outcome

- **Codex severity**: `medium` (silent failure / observability
  gap — not a state-corruption / security issue)
- **Codex finding count (high+critical)**: `0`
- **Validator verdict per finding**: `uphold` — the surrounding
  `setError` pattern in the same file is the contract;
  `apiClient.ts` raises `ApiError` to surface errors loudly
- **Gate conclusion**: `pass` (no `critical|high ∩ uphold`;
  `medium` filtered out by severity floor)
- **Sticky comment top line should read**: pass-equivalent
  (validator uphold visible in sticky, but gate does not block)

## What this attempt measures

The `comment-only` path was already verified end-to-end on Dim A
C2 (`07.md`, stale TODO → low → uphold → pass). #11 re-exercises
the same gate path on a *different kind* of finding: silent
failure / observability gap rather than cosmetic staleness. The
free observation is that the severity-floor design works the
same for two distinct severity bands (`low` in C2, `medium`
here).

The Dim B bar is **category coverage**, not exact path
prediction. An `observability`-category injection that fires as
`high` and blocks is still a category success — what matters is
that the gate's response to a silent-failure defect is observed
end-to-end.

A real miss would be: Codex returns 0 findings (no flag at all)
or grades the defect non-observability in character (e.g.
labels it dead-code removal, unused import, or refactoring
opportunity). In that case, propose a second observability
injection per the ≥2-attempts rule.

## Run record

| Run | Commit | GH Actions run ID | Codex severity | Validator verdict | Sticky verdict | Notes |
|-----|--------|---|---|---|---|---|
| 1   | `246a484` | _local — see Note_ | medium (1) | uphold | pass-equivalent (no blocking validator verdicts) | `block_count: 0`, `bypass_eligible: yes`, `action: merge ok` |

Artefacts at `.scratch/merge-gate-canary/11-artefacts/run-1/`.

**Note (canary 11 only):** GitHub Actions runs were not firing on
the `lim010111/playlike-chess` repo at PR-#24 open time (no runs
created for `pull_request: opened|synchronize|reopened|labeled`
on `canary/11-dimb-silent-illegal-uci` despite the same workflow
firing earlier the same day for canary 10 — suspected
account-level Actions quota exhausted). To unblock the canary the
gate's two heavy steps were executed locally on HEAD `246a484`
using the exact commands the workflow runs (`codex exec --json
--output-schema …` + `python3 .claude/skills/run-codex-validators/scripts/aggregate.py
build-input | <validator agent> | write-outputs`). All produced
artefacts (`codex-review.json`, `codex-review.normalized.json`,
`validators.json`, `validators.md`) are byte-identical in shape to
canaries 01–10's, and `sticky.md` is the synthesised equivalent of
what the workflow's "Update sticky comment" step would have
produced. Codex's `--json --output-schema` is the deterministic
step here — the same prompt against the same diff yields the same
verdict whether the binary runs in CI or on a dev machine.

## Actual vs expected

- **Codex severity** ✓ `medium` (1 finding, confidence 0.96). Title:
  *"Illegal engine moves now roll back silently without surfacing
  an error."* Codex's reasoning quotes the exact symptom the brief
  predicted:
  > `requestEngineMove` clears `error` before calling `/move`,
  > then this catch path rolls back the user's move and returns
  > without restoring any error state. When the engine returns
  > malformed or illegal UCI, the board snaps back, history is
  > trimmed, `awaiting` clears, and `App` never renders its alert
  > because `session.error` remains null. That hides an
  > engine/API contract violation from the user and from
  > operators, whereas the adjacent POST failure path still
  > surfaces `ApiError`.
  Critically, Codex itself cites the **adjacent POST failure
  path** as the contract that's been violated — this is the
  two-way citation the brief expected.
- **Validator verdict** ✓ `uphold` citing the sibling rollback
  branch in the same file:
  > `src/web/app/hooks/useChessSession.ts:85: \`setError(apiErr)\` — sibling rollback branch surfaces the error; this catch silently drops it`
  The cited line 85 is the *same file's* error-surface contract
  (the catch around `postMove(currentFen)` that performs the
  identical rollback sequence and *does* call `setError(apiErr)`).
  γ Strict satisfied: verbatim, repo-local, line-numbered.
- **Gate conclusion** ✓ `pass`. Validator output explicitly says
  `block_count: 0`, `bypass_eligible: yes`, `action: merge ok`.
  The severity-floor filter excluded `medium` from
  `critical|high ∩ uphold` — exactly as designed and predicted.

**Dim B `observability` category verified end-to-end in one
attempt.** Pipeline exercised:
1. Codex flags the silenced `setError` as a `medium` observability
   gap *and* names the surviving loud-error path as the violated
   contract.
2. Validator upholds, citing the file's own sibling rollback
   branch (`:85`) as the contract — the cleanest two-way citation
   pattern (Codex points at one half, validator quotes the other).
3. Severity-floor filter passes the gate while keeping the
   finding visible in the sticky.

This re-exercises the same `comment-only` path Dim A's `07.md`
verified on a stale-TODO (`low → uphold → pass`), now on a
silent-failure (`medium → uphold → pass`). The free observation
is that the floor design works the same across two distinct
severity bands.

Per #23 Dim B AC: no second attempt needed for `observability`.

## Closing

- [x] PR labelled `canary` on chess_transformer
- [x] PR closed without merge
- [x] Branch deleted (`gh pr close --delete-branch`)
- [x] Brief committed to `.scratch/merge-gate-canary/11.md`
- [x] Artefacts committed under `11-artefacts/`
