# ignore `/meetings`
docs/meetings

# Claude Code session-local state (per-worktree; not for sharing)
.claude/handoffs/
.claude/worktrees/

# Python — venv and tool caches
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# Local merge-gate run artifacts (only schema.json is tracked)
.codex-review/*
!.codex-review/schema.json

